services:
  mongodb:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: clg_management
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
